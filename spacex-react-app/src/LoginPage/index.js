import React from 'react';
import Register from '../Register';
import Login from '../Login';

function LoginPage() {
    return (
        <main>
            <Register />
            <Login />
        </main>
    );
}

export default LoginPage;